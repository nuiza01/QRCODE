/**
 * Public surface of the generator. SEO's per-type landing pages import from
 * here, so the `Generator` / `GeneratorProps` pair is a frozen contract.
 */
export { Generator, type GeneratorProps } from "./Generator";
